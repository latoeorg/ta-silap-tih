export const IsCan = code => {
  // const user = JSON.parse(localStorage.getItem("App-User"))
  // const permissions = JSON.parse(localStorage.getItem("App-Permissions"))

  // if (!permissions.find(p => p.code === code).roles.length) return true

  // return permissions.find(p => p.code === code).roles.some(r => r.role_name === user.role_name)

  return true
}
